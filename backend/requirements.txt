fastapi>=0.115.0
uvicorn[standard]>=0.32.0
supabase>=2.10.0
python-jose[cryptography]>=3.3.0
pydantic-settings>=2.6.0
anthropic>=0.40.0
langgraph>=0.2.0
deepgram-sdk>=3.7.0,<4  # v4+ is a breaking API rewrite; voice.py uses the v3 API (LiveOptions/LiveTranscriptionEvents). Revisit when LiveKit replaces voice.py.
elevenlabs>=1.9.0
python-dotenv>=1.0.0
httpx>=0.27.0

# ── Production wrapper ─────────────────────────────────────────────────────────
slowapi>=0.1.9          # per-user rate limiting on HTTP endpoints
langsmith>=0.1.100      # request tracing (no-op unless LANGSMITH_API_KEY is set)
redis>=5.0.0            # unused until settings.redis_url is set (multi-worker cache/limits)

# ── Dev / test ─────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
