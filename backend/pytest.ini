[pytest]
testpaths = tests

# `async def test_...` runs without a per-test decorator.
#
# Safe for the pre-existing suites: they are synchronous functions that call
# asyncio.run() internally, and auto mode only touches coroutine test
# functions. Added for the billing tests, which exercise async store code
# directly.
asyncio_mode = auto
