/** Identity details for the profile header — mock until real accounts carry them. */
export interface ProfileData {
  handle: string;
  joined: string;
}

export const mockProfile: ProfileData = {
  handle: 'yoshi.lifts',
  joined: 'April 2026',
};
